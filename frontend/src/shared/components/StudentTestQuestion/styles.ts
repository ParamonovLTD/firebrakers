import styled from 'styled-components';
import { Row, Typography } from 'antd';

export const QuestionTitle = styled(Typography.Text)`
  display: block;
  margin-bottom: 15px;
  font-size: 18px;
`

export const StudentAnswersWrapper = styled(Row)`
  margin-bottom: 30px;
  padding-left: 20px;
`;